export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
export PATH=$PATH:$ANDROID_HOME/platform-tools

export PATH=$PATH:$HOME/bin
export PATH=$HOME/bin:$PATH
export PATH=$HOME/.local/bin:$PATH

export EDITOR='vim'
eval "$(/home/mohammedi/.linuxbrew/bin/brew shellenv)"

# export GOPATH="$HOME/go"
# export PATH="$PATH:$GOPATH/bin"

# heroku autocomplete setup
# Disable this because it makes the shell very slow to start up
# HEROKU_AC_ZSH_SETUP_PATH=/home/mohammedi/.cache/heroku/autocomplete/zsh_setup && test -f $HEROKU_AC_ZSH_SETUP_PATH && source $HEROKU_AC_ZSH_SETUP_PATH;

export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
export PATH="$HOME/.poetry/bin:$PATH"

export GOPATH=$HOME/go
export PATH=$GOPATH:$GOPATH/bin:$PATH
export GOPATH=$GOPATH:$HOME/zest/go-tuto


# History is already set to -1 by default
# HISTSIZE=-1
# HISTFILESIZE=-1
export XDG_DATA_DIRS=/usr/share/plasma:/home/mohammedi/.local/share/flatpak/exports/share:/var/lib/flatpak/exports/share:/usr/local/share:/usr/share:/var/lib/snapd/desktop/applications/

export KUBECONFIG=$HOME/.kube/config:/var/snap/microk8s/current/credentials/client.config
